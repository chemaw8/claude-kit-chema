# Tareas — Spec NNN

<!-- Reglas: tareas de ~30 min, ordenadas por dependencia, cada una declara
     sus RF y cierra con "Hecho cuando:" verificable. Se implementa UNA tarea
     a la vez, tests primero cuando hay lógica (kit-codigo manda).
     Si una tarea no cabe en 30 min, son dos tareas.
     El gate usa una condición, no un total fijo de pruebas: suite prevista
     completa, código de salida 0 y sin fallos ni errores. Conteos solo como
     evidencia con fecha YYYY-MM-DD, nunca como criterio. -->

- [ ] T1. <qué>. (RF-x, RF-y)
      Hecho cuando: <comando> → <condición de éxito verificable, no conteo de pruebas>.
- [ ] T2. ...
